language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sizzle-jekyll' ]; then cd .. && eval "mv $currentfolder generator-sizzle-jekyll" && cd generator-sizzle-jekyll; fi
